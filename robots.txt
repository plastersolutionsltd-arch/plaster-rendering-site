# Robots.txt for P&R Solutions Website
# Allow all search engines to crawl the site

User-agent: *
Allow: /

# Site content is fully crawlable — we want AI crawlers indexing the pages.
# Only the private working docs and dev scripts are off limits.
Disallow: /*.md$
Disallow: /*.py$

# Sitemap location
Sitemap: https://www.plasterandrenderingsolutions.co.uk/sitemap.xml
